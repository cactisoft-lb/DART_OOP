import 'shape.dart';

import './shape.dart';

class Rectangle extends Shape {
  // String ? color ;
  // double ? area ;
  // double ? perimeter;

  double? width;
  double? height;

  Rectangle({ this.width, this.height }) : super(

  );
}
